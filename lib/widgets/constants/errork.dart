import 'package:flutter/material.dart';

class ErrorK extends StatelessWidget {
  const ErrorK({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('An error occured'),
    );
  }
}
